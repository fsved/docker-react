sudo: required
language: generic

services:
  - docker

before_install:
  - docker build -t fsved/docker-react -f Dockerfile.dev .

script:
  - docker run -e CI=true fsved/docker-react npm run test -- --coverage